{% set name = "globus-cli" %}
{% set version = "3.14.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/globus-cli-{{ version }}.tar.gz
  sha256: 09ebf80575697d257e458881cbaa81fef0a76172deb7d2acba1380d5f691e418

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  entry_points:
    - globus = globus_cli:main

requirements:
  host:
    - python >=3.7
    - pip
  run:
    - python >=3.7
    - globus-sdk 3.20.1
    - click >=8.0.0,<9
    - jmespath 1.0.1
    - packaging >=17.0
    - requests >=2.19.1,<3.0.0
    - cryptography >=3.3.1,<37
    # for all python to keep the recipe noarch: python
    - typing-extensions >=4.0

test:
  requires:
    - pip
  imports:
    - globus_cli
    - globus_cli.commands

  commands:
    - globus --help
    - pip check

about:
  home: https://github.com/globus/globus-cli
  license: Apache-2.0
  license_family: APACHE
  license_file: LICENSE
  summary: Globus CLI
  description: A Command Line Interface to Globus.
  doc_url: https://globus.github.io/globus-cli
  dev_url: https://github.com/globusonline/globus-cli

extra:
  recipe-maintainers:
    - xylar
    - lukaszlacinski
